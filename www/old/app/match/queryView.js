//
// The query view for the match game
//

define(['jquery', 'backbone', 'app/match/queryModel'], function ($, Backbone, QueryView) {

    var QueryView = Backbone.View.extend({
    });

    return QueryView;
});
