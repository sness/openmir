//
// The main view for the match game
//

define(['jquery', 'backbone', 'app/match/gameModel'], function ($, Backbone, GameModel) {

    var GameView = Backbone.View.extend({
    });

    return GameView;
});
